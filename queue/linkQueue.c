#include <stdio.h>
#include "include.h"


void initLinkQueue(LinkQueue *queue, LinkQueue **head, LinkQueue **tail)
{
	queue->next = NULL;
	*head = *tail = queue;	
}

int pushLinkStack(LinkQueue *queue, LinkQueue **head, DataType x)
{
	
}















